feat: blog (text + images + video) — admin CMS + public pages + API

Schema: BlogPost (Markdown content, cover bytes in DB, DRAFT/PUBLISHED) +
BlogMedia; migration blog.

API
- Public: GET /blog, GET /blog/:slug (published only), GET /blog-cover/:id,
  GET /blog-media/:id
- Admin (/admin/blog, guarded): list/get/create/update/delete, POST /:id/cover,
  POST /media (returns a URL to embed). All reachable with an API key too.

Web
- Public /blog (cards) + /blog/[slug] (server-rendered: metadata, OG, Article
  JSON-LD, cover, Markdown body). "Blog" link in the header; blog in sitemap.
- Markdown renderer now supports images ![alt](url), links, and video embeds
  (!video <url> -> YouTube/Aparat iframe or .mp4 <video>) — XSS-safe (http(s) only).
- Admin "Blog" tab: list + editor (title, slug, excerpt, Markdown content,
  status, cover upload, media upload that copies a ready ![image](url) snippet).

Verified: API typecheck + smoke (116 checks incl. draft-hidden, publish, public
list/detail, media upload+serve) + next build (all blog routes) + lint, all green.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
