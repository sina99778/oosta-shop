feat: AI agent inside the Telegram bot (Gemini function-calling)

The admin can now just message the bot in plain language and an agent executes
the work end-to-end.

- lib/agent.ts: Gemini function-calling loop with in-process tools that run the
  real services — list/create/update products, plans, inventory, categories,
  stats, and create/update/list blog posts (each validated with the existing Zod
  schemas). System prompt enforces ascii slugs, product types, Persian content,
  and a "create product -> plan -> inventory" flow.
- bot.ts: any non-command text message is treated as an instruction -> runAgent;
  shows typing, returns a short Persian summary. /start hints how to use it.
- Reuses GEMINI_MODEL (configurable); disabled gracefully without a key.

Verified: API typecheck + smoke (116) + lint, all green. (Bot/agent run in the
API process; not exercised by the in-process smoke.)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
