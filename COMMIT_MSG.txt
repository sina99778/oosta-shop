feat(admin): fully editable plan rows (label, price, sale, active, delete)

The plans section previously only let you edit the sale price. Each plan is now
a full inline editor: label, price, sale price, active toggle, Save, and Delete
(delete surfaces the "deactivate instead" message when the plan still has
inventory/orders).

Verified: next build green + lint clean.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
